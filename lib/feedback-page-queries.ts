/** Query key pour la customisation de la page Feedback (titre, helpText, reviewTag, reviewTitle, reviewComment). */
export const FEEDBACK_PAGE_QUERY_KEY = [
  "customization",
  "feedback-page",
] as const;
