/** Clé TanStack Query pour les global styles. Partagée entre useGlobalStylesForm et PreviewLayout pour réutiliser le cache. */
export const GLOBAL_STYLES_QUERY_KEY = ["global-styles"] as const;

/** Clé TanStack Query pour le logo (global styles). Partagée entre useGlobalStylesForm et PreviewLayout pour réutiliser le cache. */
export const GLOBAL_STYLES_LOGO_QUERY_KEY = ["global-styles", "logo"] as const;
